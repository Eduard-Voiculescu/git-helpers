# git-helpers

Various helpers for git

Add the files under `/usr/local/bin/` and then you can run `git <your-file-name>`.

Example:

```bash
cp git-delete-localonly-branches.sh /usr/local/bin/git-delete-localonly-branches
sudo chmod +x /usr/local/bin/git-delete-localonly-branches
```
